apiVersion: v1
kind: Service
metadata:
  name: drippycutz-backend
spec:
  selector:
    app: drippycutz-backend
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
